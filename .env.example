# Example env vars (do not commit real secrets)
# NEXT_PUBLIC_APP_NAME=web-template

# Mailing list provider (reserved for future providers).
# Defaults to "console" when not set.
# MAILING_LIST_PROVIDER=console
